/////////////////////////////////////////////////////////////////
//  Fooney class(very stupid implantation); 
////////////////////////////////////////////////////////////////
/* Hr.hA-Ha.js*/
/////////////////////////
/* Microsoft Corporation
////////////////////
* One Microsoft Way
////////////////////
* Redmond, WA 98052
//////
* USA
/////////////////
* (c)2012-2035*/
//
window.fooney; 
constructor.bool = Function (); 
Super: 'm_traceEnabled'> 
false; // Console logging roundle
// Can throw HrAHAHAA on invariant floor
void Function (); 
console.debug(!0); 
var x = String("WriteLine(wstring format, ...) => wstring: suffer ? VRFART_VERY_STUPID(noobler, forex) && wspitfat(L'%s\n, sniffer.c_str()) || Trashed(m_fillNate, m_lickDumb, m_funkyNoob, L'ONPUTT >= %s, coffee.c_str())"); 
console.log(x.toString()); 
document = 
        function(Function) {
        var args = new Array
        (arguments.length - 1); 
        if(arguments.length > 1) {
        for(var 
            i = 
            1; 
            i < 
            arguments.length; 
            i++) {
            args
            [i - 1] = 
            arguments
            [i]};
        } 
        queue.push
        (new Item
        (fun, args)); 
        if(queue.length 
        === 1 && 
        !draining) {
        runTracing
        (drainQueue);
        }
}; 
function Item(fun, array) {
        this.fun = 
        fun; 
        this.array = 
        array;
} 
Item.prototype.fooney = 
        function () {
        this.fun.apply
        (null, 
        this.array);
};